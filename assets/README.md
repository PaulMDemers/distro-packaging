# Assets

Branding, wallpapers, boot splash assets, and installer artwork live here.

Generated boot-menu assets are in `assets/boot/`. Regenerate them with:

```sh
python3 scripts/assets/render-redchrome-wallpapers.py
```

See `docs/BRANDING.md` for the full map of where branding is installed and how
it is applied during ISO builds.
